/* ========================================
   SERVICEHUB BACKEND CONFIGURATION
======================================== */

/*
   Put your deployed Node.js backend base URL here.

   Example:
   window.SERVICEHUB_BACKEND_URL =
       "https://your-backend.example.com";

   Leave empty while the backend is not deployed.
*/
window.SERVICEHUB_BACKEND_URL = "";
